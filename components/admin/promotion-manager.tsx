"use client";

import Image from "next/image";
import { ImagePlus, Pencil, Save, Trash2, X } from "lucide-react";
import { useState } from "react";
import type { Promotion } from "@/lib/types";
import { deletePromotion, savePromotion } from "@/lib/actions/admin";
import { uploadVictoriaMedia } from "@/components/admin/media-upload";
import { Button } from "@/components/ui/button";

type Draft = { id?: string; title_ar: string; description_ar: string; image_url: string | null; storage_path: string | null; button_text_ar: string; button_link: string; placement: Promotion["placement"]; start_at: string; end_at: string; is_active: boolean; sort_order: number };
const empty: Draft = { title_ar: "", description_ar: "", image_url: null, storage_path: null, button_text_ar: "", button_link: "", placement: "homepage", start_at: "", end_at: "", is_active: true, sort_order: 0 };
export function PromotionManager({ promotions }: { promotions: Promotion[] }) { const [draft, setDraft] = useState<Draft>(empty); const [file, setFile] = useState<File | null>(null); const [status, setStatus] = useState(""); const [saving, setSaving] = useState(false); const setField = <K extends keyof Draft>(key: K, value: Draft[K]) => setDraft((current) => ({ ...current, [key]: value })); async function save(event: React.FormEvent) { event.preventDefault(); setSaving(true); setStatus(""); try { if (!draft.title_ar) throw new Error("أضيفي عنوان العرض"); let imageUrl = draft.image_url; let storagePath = draft.storage_path; if (file) { const upload = await uploadVictoriaMedia(file, "promotions"); imageUrl = upload.url; storagePath = upload.path; } await savePromotion({ id: draft.id, title_ar: draft.title_ar, description_ar: draft.description_ar || null, image_url: imageUrl, storage_path: storagePath, button_text_ar: draft.button_text_ar || null, button_link: draft.button_link || null, placement: draft.placement, start_at: draft.start_at ? new Date(draft.start_at).toISOString() : null, end_at: draft.end_at ? new Date(draft.end_at).toISOString() : null, is_active: draft.is_active, sort_order: draft.sort_order }); window.location.reload(); } catch (error) { setStatus(error instanceof Error ? error.message : "تعذر حفظ العرض"); } finally { setSaving(false); } } function edit(item: Promotion) { setDraft({ id: item.id, title_ar: item.title_ar, description_ar: item.description_ar ?? "", image_url: item.image_url, storage_path: item.storage_path, button_text_ar: item.button_text_ar ?? "", button_link: item.button_link ?? "", placement: item.placement, start_at: item.start_at?.slice(0, 16) ?? "", end_at: item.end_at?.slice(0, 16) ?? "", is_active: item.is_active, sort_order: item.sort_order }); } async function remove(item: Promotion) { if (!window.confirm("هل تريدين حذف هذا العرض؟")) return; try { await deletePromotion(item.id, item.storage_path); window.location.reload(); } catch { setStatus("تعذر حذف العرض"); } } return <div className="grid gap-8 lg:grid-cols-[.85fr_1.15fr]"><form onSubmit={save} className="rounded-[1.5rem] bg-cream p-6"><div className="flex items-center justify-between"><h2 className="font-serif text-3xl">{draft.id ? "تعديل العرض" : "عرض جديد"}</h2>{draft.id && <button type="button" onClick={() => { setDraft(empty); setFile(null); }} aria-label="إلغاء"><X size={18} /></button>}</div><div className="mt-6 space-y-4"><label className="text-xs text-ink/55">العنوان<input required value={draft.title_ar} onChange={(e) => setField("title_ar", e.target.value)} className="form-input" /></label><label className="text-xs text-ink/55">الوصف<textarea value={draft.description_ar} onChange={(e) => setField("description_ar", e.target.value)} rows={3} className="form-input resize-none" /></label><div className="grid grid-cols-2 gap-3"><label className="text-xs text-ink/55">نص الزر<input value={draft.button_text_ar} onChange={(e) => setField("button_text_ar", e.target.value)} className="form-input" /></label><label className="text-xs text-ink/55">الرابط<input value={draft.button_link} onChange={(e) => setField("button_link", e.target.value)} className="form-input" placeholder="/shop" /></label></div><div className="grid grid-cols-2 gap-3"><label className="text-xs text-ink/55">الموضع<select value={draft.placement} onChange={(e) => setField("placement", e.target.value as Promotion["placement"])} className="form-input"><option value="homepage">الصفحة الرئيسية</option><option value="cakes">الكيك</option><option value="chocolate">الشوكولاتة</option></select></label><label className="text-xs text-ink/55">الترتيب<input type="number" value={draft.sort_order} onChange={(e) => setField("sort_order", Number(e.target.value))} className="form-input" /></label></div><div className="grid grid-cols-2 gap-3"><label className="text-xs text-ink/55">يبدأ في<input type="datetime-local" value={draft.start_at} onChange={(e) => setField("start_at", e.target.value)} className="form-input" /></label><label className="text-xs text-ink/55">ينتهي في<input type="datetime-local" value={draft.end_at} onChange={(e) => setField("end_at", e.target.value)} className="form-input" /></label></div><label className="toggle-label"><input type="checkbox" checked={draft.is_active} onChange={(e) => setField("is_active", e.target.checked)} /> العرض ظاهر للزوار</label><label className="inline-flex cursor-pointer items-center gap-2 rounded-xl border hairline px-4 py-3 text-xs"><ImagePlus size={16} /> رفع أو استبدال الصورة<input type="file" accept="image/jpeg,image/png,image/webp" className="hidden" onChange={(e) => setFile(e.target.files?.[0] ?? null)} /></label></div>{status && <p className="mt-4 text-xs text-burgundy">{status}</p>}<Button disabled={saving} className="mt-6">{saving ? "جارٍ الحفظ..." : <><Save size={16} /> حفظ العرض</>}</Button></form><div className="space-y-4">{promotions.map((item) => <div key={item.id} className="flex gap-4 rounded-2xl bg-cream p-4"><div className="relative h-20 w-20 shrink-0 overflow-hidden rounded-xl bg-[#eee7dc]">{item.image_url && <Image src={item.image_url} alt={item.title_ar} fill sizes="80px" className="object-cover" />}</div><div className="min-w-0 flex-1"><p className="font-medium">{item.title_ar}</p><p className="mt-1 text-xs text-ink/45">{item.placement === "homepage" ? "الصفحة الرئيسية" : item.placement} · {item.is_active ? "ظاهر" : "مخفي"}</p><div className="mt-3 flex gap-1"><button type="button" onClick={() => edit(item)} className="rounded-full p-2 text-ink/50 hover:bg-ink/5" aria-label="تعديل"><Pencil size={15} /></button><button type="button" onClick={() => remove(item)} className="rounded-full p-2 text-ink/50 hover:bg-red-50 hover:text-red-700" aria-label="حذف"><Trash2 size={15} /></button></div></div></div>)}{!promotions.length && <p className="rounded-2xl bg-cream p-8 text-center text-sm text-ink/45">لا توجد عروض حالياً.</p>}</div></div>; }
