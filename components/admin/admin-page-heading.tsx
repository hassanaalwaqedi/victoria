export function AdminPageHeading({ eyebrow, title, description, action }: { eyebrow: string; title: string; description?: string; action?: React.ReactNode }) { return <div className="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="eyebrow">{eyebrow}</p><h1 className="mt-3 font-serif text-4xl tracking-[-.04em] sm:text-5xl">{title}</h1>{description && <p className="mt-3 text-sm text-ink/55">{description}</p>}</div>{action}</div>; }
