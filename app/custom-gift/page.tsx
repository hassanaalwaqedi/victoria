import Link from "next/link";
import { ArrowLeft, Box, Flower2, MessageCircle, Sparkles } from "lucide-react";
import { PageHero } from "@/components/layout/page-hero";
import { getHomepageSections, getSection } from "@/lib/content";
export default async function CustomGiftPage() { const section = getSection(await getHomepageSections(), "gift"); return <><PageHero eyebrow="تجربة هدايا مخصصة" title={section?.title_ar ?? "اصنع هديتك"} description={section?.subtitle_ar ?? undefined} /><section className="container-wide py-12 sm:py-20"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[[Box,"اختاري البوكس","أحجام وتفاصيل مختلفة"],[Sparkles,"أضيفي الشوكولاتة","نكهات تحبونها"],[Flower2,"وردة وبطاقة","لمسة دافئة"],[MessageCircle,"رسالتك الشخصية","كلمات من القلب"]].map(([Icon,title,sub]) => { const I = Icon as typeof Box; return <div key={title as string} className="rounded-[1.5rem] bg-white/60 p-7"><I className="text-gold" size={25} /><h2 className="mt-8 text-lg">{title as string}</h2><p className="mt-2 text-xs text-ink/50">{sub as string}</p></div>; })}</div>{section?.button_text_ar && section.button_link && <Link href={section.button_link} className="mt-10 inline-flex items-center gap-2 rounded-full bg-ink px-6 py-3 text-sm text-cream">{section.button_text_ar}<ArrowLeft size={16} /></Link>}</section></>; }
